# Legacy files

This folder keeps the original pre-release script for reference and backwards compatibility research.

- `qr_2025_v.0.5.py` — original script preserved for history only. The maintained CLI/library entry point is `qr_crypto.py`; the maintained GUI is `qr_gui_tk.py`.

Do not use legacy files as the primary application entry point.
